import { ReactNode } from "react";
interface FormCanLeaveProps {
    children?: ReactNode;
    canLeave?: boolean;
}
export default function FormCanLeave({ children, canLeave, }: FormCanLeaveProps): ReactNode;
export {};
