import { Organisation } from "@/types/application";
interface OrganisationDetailsProps {
    isApproved: boolean;
    data: Organisation;
}
export default function OrganisationDetails({ isApproved, data, }: OrganisationDetailsProps): import("react/jsx-runtime").JSX.Element;
export {};
