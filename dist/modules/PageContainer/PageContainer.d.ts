import { ReactNode } from "react";
interface PageContainerProps {
    children: ReactNode;
}
declare function PageContainer({ children }: PageContainerProps): import("react/jsx-runtime").JSX.Element;
export default PageContainer;
