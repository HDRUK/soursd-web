import { ReactNode } from "react";
interface PageTitleProps {
    children: ReactNode;
}
declare function PageTitle({ children }: PageTitleProps): import("react/jsx-runtime").JSX.Element;
export default PageTitle;
