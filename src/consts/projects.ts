enum ProjectDetailsAccessType {
  TRE = "TRE",
  RELEASE = "RELEASE",
}

enum RequestFrequency {
  ONE_OFF = "ONE-OFF",
  RECURRING = "RECURRING",
}

export { ProjectDetailsAccessType, RequestFrequency };
